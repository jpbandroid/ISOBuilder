<Page
    x:Class="ISOBuilder.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <RelativePanel Grid.Row="0">
            <TextBlock
    Text="ISO Builder"
                RelativePanel.AlignLeftWithPanel="True" Margin="15, 5, 15, 15"/>
        </RelativePanel>

        <Grid Grid.Row="1">
            <AppBarButton Icon="Setting" Click="AppBarButton_Click" HorizontalAlignment="Right"></AppBarButton>
            <Button Content="Build ISO" FontSize="24" Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        
    </Grid>
</Page>
